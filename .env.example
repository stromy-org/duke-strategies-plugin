# Deliverable canvas MCP URL (provider-portable canvas backend for strategic skills).
# Auth is per-user OAuth via Microsoft Entra ID — handled by the MCP client / mcp-remote
# at first use (browser flow). No client-side secret required.
# See README "Canvas UX" + infra-docs/ai/deliverable-canvas.md for context.
DELIVERABLE_CANVAS_MCP_URL=
