{
  "$schema": "https://gofastmcp.com/public/schemas/mcp-config.schema.json",
  "_notes": "Canonical MCP server config for duke-strategies-plugin. Edit only this file. Run scripts/render-mcp.py to regenerate .mcp.json / .gemini/settings.json / .codex/config.toml / .vscode/mcp.json.",
  "servers": {
    "deliverable-canvas": {
      "transport": "http",
      "url": "${DELIVERABLE_CANVAS_MCP_URL}",
      "_consumed_by": ["claude", "codex", "gemini"],
      "_codex_via": "mcp-remote",
      "_gemini_via": "mcp-remote",
      "_notes": "Shared canvas backend for strategic deliverables (proposal, messaging-framework, press-release). Per-user OAuth via Microsoft Entra ID (handled by mcp-remote browser flow). See infra-docs/ai/deliverable-canvas.md."
    }
  }
}
